
unit tests
coverage
create locks on blob files
check of blob file already exists
configurable locations for interim files
all errors print the image ref - minimall review all errors
rationalize these
  func (p *Puller) PullManifest(mpt ManifestPullType)
  func (p *Puller) HeadManifest()
  func (p *Puller) GetManifest()
  func (p *Puller) GetManifestByDigest(digest string)
